# Copy to .cretli-sdk.env (gitignored) if you use the Cursor SDK harness.
# Cursor → Dashboard → Integrations → API keys
CURSOR_API_KEY=

# Optional default model when the UI selection is "auto"
# CURSOR_SDK_DEFAULT_MODEL=composer-2
